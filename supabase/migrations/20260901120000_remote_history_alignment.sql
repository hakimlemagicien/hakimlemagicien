-- History alignment stub.
-- This version exists on Production remote migration history but was missing from the local
-- migrations directory, which blocked `supabase db push`.
-- Content is intentionally a no-op: Production already applied the original remote migration.
-- Do not invent schema here.

SELECT 1;
